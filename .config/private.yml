# DevForge 私有配置

> 内部配置，不对外公开

---

## 🔐 GitHub 配置

```yaml
repository:
  name: devforge
  visibility: private
  owner: Y1-q-1Q
  
access:
  - tinyfish: admin
  
collaborators: []
# 需明确授权后才添加
```

---

## 🗄️ 数据库配置（生产环境）

```yaml
# 待配置
postgresql:
  host: (待填写)
  port: 5432
  database: devforge
  username: (待填写)
  password: (待填写)
  
redis:
  host: (待填写)
  port: 6379
  password: (待填写)
  
meilisearch:
  host: (待填写)
  port: 7700
  api_key: (待填写)
```

---

## 🤖 AI 配置

```yaml
openai:
  api_key: (待填写)
  organization: (待填写)
  
ollama:
  host: http://localhost:11434
  models:
    - codellama
    - llama2
    
embedding:
  model: sentence-transformers/all-MiniLM-L6-v2
  device: cuda
```

---

## ☁️ 云服务配置

```yaml
# 阿里云
aliyun:
  access_key_id: (待填写)
  access_key_secret: (待填写)
  region: cn-hangzhou
  
# 腾讯云
tencent:
  secret_id: (待填写)
  secret_key: (待填写)
  region: ap-guangzhou
  
# AWS
aws:
  access_key_id: (待填写)
  secret_access_key: (待填写)
  region: ap-northeast-1
```

---

## 🔧 部署配置

```yaml
# 服务器
servers:
  production:
    host: (待填写)
    user: (待填写)
    ssh_key: (待填写)
    
  staging:
    host: (待填写)
    user: (待填写)
    ssh_key: (待填写)

# Kubernetes
k8s:
  cluster: (待填写)
  namespace: devforge
  context: (待填写)
```

---

## 📊 监控配置

```yaml
# 日志
logging:
  level: info
  format: json
  destination: (待填写)
  
# 监控
monitoring:
  prometheus: (待填写)
  grafana: (待填写)
  
# 告警
alerting:
  slack_webhook: (待填写)
  email: (待填写)
```

---

## 📝 备注

- 所有敏感信息使用环境变量或密钥管理服务
- 生产环境配置不提交到Git
- 使用 `.env` 文件或 Kubernetes Secrets
- 定期轮换密钥

---

**机密等级**: 内部机密
**最后更新**: 2026-03-22